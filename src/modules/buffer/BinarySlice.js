/**
 * @module BinarySlice
 */


class BinarySlice {
	/**
	 * 
	 * @param {number} start 
	 * @param {number} length 
	 */
	constructor(start, length) {
		this.start = start;
		this.length= length;
	}
}

export default BinarySlice;